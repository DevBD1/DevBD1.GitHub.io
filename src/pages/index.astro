---
import Layout from '../layouts/Layout.astro';
import SkillTicker from '../components/SkillTicker';

---

<Layout>
  <!-- Ticker at the top for maximum visibility -->
  <SkillTicker client:load />

  <main className="container mx-auto px-4 py-16">
    <div className="flex flex-col items-center justify-center min-h-[60vh] text-center space-y-8">
      
      <!-- Hero Section -->
      <h1 className="text-6xl font-bold tracking-tighter">
        <span className="text-slate-200">Burak</span>
        <span className="text-[var(--color-primary)]">.Dorman</span>
      </h1>
      
      <p className="text-xl text-slate-400 max-w-2xl">
        FinTech Engineer & Full Stack Developer based in Istanbul.
        <br />
        Building decentralized systems and scalable applications.
      </p>

      <div className="flex gap-4">
        <a href="#projects" className="px-6 py-3 bg-[var(--color-primary)] text-slate-900 font-bold rounded hover:opacity-90 transition-opacity">
          View Proof of Work
        </a>
        <a href="#contact" className="px-6 py-3 border border-slate-700 rounded hover:bg-slate-800 transition-colors">
          Read Contract
        </a>
      </div>

    </div>

    <!-- Proof of Work (Placeholder) -->
    <section id="projects" className="py-20">
      <h2 className="text-3xl font-bold mb-8 font-mono border-b border-slate-800 pb-4">
        > Proof_of_Work
      </h2>
      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
        {/* Project Cards will go here */}
        <div className="bg-slate-900 border border-slate-800 p-6 rounded hover:border-[var(--color-primary)] transition-colors group">
          <div className="flex justify-between items-start mb-4">
            <h3 className="text-xl font-bold group-hover:text-[var(--color-primary)]">Relay Layer</h3>
            <span className="font-mono text-xs text-slate-500">Block #202602</span>
          </div>
          <p className="text-slate-400 mb-4">Multi-agent orchestration system for task delegation and monitoring.</p>
          <div className="flex gap-2">
            <span className="text-xs bg-slate-800 px-2 py-1 rounded font-mono">Node.js</span>
            <span className="text-xs bg-slate-800 px-2 py-1 rounded font-mono">SQLite</span>
          </div>
        </div>

         <div className="bg-slate-900 border border-slate-800 p-6 rounded hover:border-[var(--color-primary)] transition-colors group">
          <div className="flex justify-between items-start mb-4">
            <h3 className="text-xl font-bold group-hover:text-[var(--color-primary)]">WickdBot</h3>
            <span className="text-xs text-slate-500 font-mono">Block #202511</span>
          </div>
          <p className="text-slate-400 mb-4">Algorithmic trading bot applying smart money concepts to crypto markets.</p>
          <div className="flex gap-2">
            <span className="text-xs bg-slate-800 px-2 py-1 rounded font-mono">Python</span>
            <span className="text-xs bg-slate-800 px-2 py-1 rounded font-mono">Binance API</span>
          </div>
        </div>
      </div>
    </section>

  </main>
</Layout>
